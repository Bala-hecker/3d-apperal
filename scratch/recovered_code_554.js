"# Admin Control Panel for Limited Edition Drops Calendar\n\nImplement a dynamic dashboard interface within the Admin Panel that enables administrators to manage the storefront launch calendar.\n\n## Proposed Changes\n\n### Database Layer\n\n#### [MODIFY] [schema.sql](file:///e:/APP%203D/3d-apparel-studio/schema.sql)\n- Append the database schema definition for the `storefront_drops` table.\n- Disable row-level security (RLS) on this table to align with the rest of the application's storefront settings.\n- Seed the table with default streetwear drops so that it loads mock data immediately.\n\n---\n\n### Admin Component Layer\n\n#### [MODIFY] [src/app/admin/page.js](file:///e:/APP%203D/3d-apparel-studio/src/app/admin/page.js)\n- **State Hooks**:\n  - Add state hooks for `dropsList`, `loadingDrops`, `editingDrop`, `savingDrop`, and `dropFile`.\n- **API Call Logic**:\n  - Add `fetchStorefrontDrops()` to read custom items from the `storefront_drops` table, falling back to local defaults if empty or query errors out.\n  - Add `handleSaveStorefrontDrop()` to upsert drop edits, including support for file uploads (storing them in the `drops/` path of the `product-assets` storage bucket) and raw path URLs.\n  - Add `handleDeleteStorefrontDrop()` to confirm and delete drops by ID.\n  - Update `checkDatabaseSchema()` to inspect the database for the existence of `storefront_drops` table.\n- **Triggers**:\n  - Fetch storefront drops during initial dashboard authentication verification mount.\n  - Refetch storefront drops when the \"Storefront Settings\" tab is clicked.\n- **UI Components**:\n  - Extend the Sub-tab switcher in the Storefront settings page with a **Drops Calendar** button.\n  - Create the sub-panel UI for the drops list displaying displays order, images, titles, prices, status tags, and countdown times.\n  - Add a form to create/edit drops including fields for title, price, description, status selection, and a `datetime-local` input for target date.\n\n---\n\n## Verification Plan\n\n### Manual Verification\n1. 
<truncated 739 bytes>